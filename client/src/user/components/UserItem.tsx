const UserItem: React.FC<{
    id: string;
    image: string;
    name: string;
    placeCount: number;
}> = ({ id, image, name, placeCount }) => {
    return <div>UserItem </div>;
};

export default UserItem;
